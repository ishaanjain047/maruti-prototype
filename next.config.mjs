/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  // For GitHub Pages with repo name in URL (uncomment if needed)
  // basePath: '/maruti-prototype',
  // assetPrefix: '/maruti-prototype',
};

export default nextConfig;
